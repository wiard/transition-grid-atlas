# KTA TikZ Sources

Deze map bevat TikZ/LaTeX-bronbestanden voor KTA-figuren.

Compileer voorbeeld:

```bash
pdflatex kta_reversibility_ensemble.tex
pdflatex kta_three_layer_architecture.tex
```

De PDF/PNG-exporten staan in `../figures/`.
